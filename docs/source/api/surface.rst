Surface workflows
=================

Slab configuration, deposition, and validation for supported-cluster runs.

.. automodule:: scgo.surface
   :members:
     SurfaceSystemConfig,
     make_surface_config,
     make_graphite_surface_config,
     describe_surface_config,
   :undoc-members:
   :show-inheritance:

Composition builders for campaigns live in :mod:`scgo.run_minima` (``build_one_element_compositions``,
``build_two_element_compositions``); high-level runners are in :doc:`/api/runner_api`.
